import 'package:get/get.dart';
import 'package:sizer/sizer.dart';

// * Width
final double k10Width = Get.width * 0.024;
final double k60Width = Get.width * 0.14;
final double k80Width = Get.width * 0.19;
final double k70Width = Get.width * 0.17;

// * Height
final double k20Height = Get.height * 0.02;
final double k30Height = Get.height * 0.04;
final double k40Height = Get.height * 0.05;
final double k10Height = Get.height * 0.014;

// * Radius
final double k35Radius = Get.width * 0.085;

// * Icons
final double k40Icon = SizerUtil.deviceType == DeviceType.mobile ? 40.0 : 55.0;

// * Fonts
final double k20Font = SizerUtil.deviceType == DeviceType.mobile ? 20.0 : 30.0;
final double k18Font = SizerUtil.deviceType == DeviceType.mobile ? 18.0 : 28.0;

// * Strings
const String kAppTitle = 'All';
const String kAppSubtitle = 'Tasks';

/**
 * ! Nexus 6 pixel dimensions
 * * width: 411
 * * height: 707
 */
